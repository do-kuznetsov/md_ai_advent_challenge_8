# Nomic Embed Text ONNX assets

Place local ONNX embedding files here:

```text
rag/models/nomic-embed-text/
  model.onnx
  tokenizer.json
```

`model.onnx` is ignored by git because it is too large for the repository.
